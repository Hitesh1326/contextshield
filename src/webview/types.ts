/**
 * Webview-side type aliases (re-export protocol types for React components).
 */
export type {
  ExtensionToWebviewMessage,
  WebviewToExtensionMessage,
  WebviewConfigSnapshot,
  WebviewModelStatus
} from "../shared/webviewProtocol";
